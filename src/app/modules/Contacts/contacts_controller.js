import { _ } from '../Start/start_controller';
import { Contact } from './contact_helper';

/**
 * Tests for Jest
 */ 
export const Tests = {
    Setup: async () => {
        page = _.GetPage();
    },
        
    T1: async () => {
        
    }
}